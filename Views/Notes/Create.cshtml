@model NotesApp.Models.Note
@{
    ViewData["Title"] = "Create Note";
}

<div style="max-width:680px; margin:0 auto;">
    <h1 class="page-title">New Note</h1>
    <p class="page-subtitle">Write something worth remembering</p>

    <div class="card">
        <form asp-action="Create" method="post">
            @Html.AntiForgeryToken()
            <div class="form-group">
                <label asp-for="Title">Title</label>
                <input asp-for="Title" type="text" placeholder="Note title..." />
                <span asp-validation-for="Title" style="color:var(--danger); font-size:0.82rem;"></span>
            </div>
            <div class="form-group">
                <label asp-for="Category">Category</label>
                <select asp-for="Category">
                    @foreach (var cat in ViewBag.Categories as List<string>)
                    {
                        <option value="@cat">@cat</option>
                    }
                </select>
            </div>
            <div class="form-group">
                <label asp-for="Content">Content</label>
                <textarea asp-for="Content" placeholder="Start writing..."></textarea>
                <span asp-validation-for="Content" style="color:var(--danger); font-size:0.82rem;"></span>
            </div>
            <div style="display:flex; gap:0.75rem;">
                <button type="submit" class="btn btn-primary">Save Note</button>
                <a href="/Notes" class="btn btn-secondary">Cancel</a>
            </div>
        </form>
    </div>
</div>
